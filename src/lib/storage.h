#ifndef egpu_x13_lib_storage_h_
#define egpu_x13_lib_storage_h_

/*

gpu_state
id(bool), active_gpu(smallint), vendor_info(varchar)

curr_tx
id(bool), tx_id(uuid), timestamp, from, to, status

tx_ops
id(uuid), op_id, op_name, op_status, error_log

*/

#endif